# Dart / Flutter
.dart_tool/
.packages
build/

# 包的锁文件不入库（example 是应用，它的 pubspec.lock 要提交）
/pubspec.lock

# IDE
*.iml
.idea/

# 示例的平台壳子按需生成，不入库：
#   cd example && flutter create --platforms=linux .    # 或 android / windows / macos / web
example/linux/
example/android/
example/ios/
example/macos/
example/windows/
example/web/
